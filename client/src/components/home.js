import React from 'react';
import GeneratorForm from './generator_form'
import SessionInfo from './session_info'

const Home = () => (
  <div className="home">
    <GeneratorForm />
    <SessionInfo />
  </div>
);

export default Home;
